{
  "version": 2,
  "builds": [
    {
      "src": "api/main.go",
      "use": "@vercel/go",
      "config": {
        "buildTags": ["vercel"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/main.go"
    }
  ]
}